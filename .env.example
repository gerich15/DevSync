# GitHub OAuth (required for login)
# Create app: https://github.com/settings/developers
# Callback URL в настройках приложения: http://localhost:8181/api/auth/github/callback (локально) или ваш URL
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
# Optional; default http://localhost:8181/api/auth/github/callback
# GITHUB_REDIRECT_URL=http://localhost:8181/api/auth/github/callback

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Локальный backend (чтобы не конфликтовать с Docker на 8180)
SERVER_PORT=8181

# Optional: Notifications
TELEGRAM_BOT_TOKEN=
DISCORD_WEBHOOK_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
